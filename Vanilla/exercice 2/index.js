// Exercice 2 : manipulation de chaînes de caractères :


function reverse(sentence) {
    let sentenceReversed;
    // ton code ici : tu reçois une phrase en parametre, je veux la renvoyer en ayant 
    // tous les caractères affichés dans l'ordre inverse ... ex : "chien" je récupère:
    // "neihc". 
    // tu peux demander à google s'il existe des méthodes internes à Javascript pour te permettre
    // de réaliser cela facilement.
    return console.log(sentenceReversed);
}


reverse('Il était une fois');
// ... Attendu : siof enu tiaté lI
reverse(`c'est à la wild, qu'on code, qu'on code, c'est à la wilde qu'on code le plus fort`);
// ... Attendu : trof sulp el edoc no'uq edliw al à tse'c ,edoc no'uq ,edoc no'uq ,dliw al à tse'c 
reverse('Je suis un génie');
// ...  Attendu: einég nu sius eJ

