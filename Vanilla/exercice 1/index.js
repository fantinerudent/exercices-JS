// Exercice 1 :

// Ecrire une fonction qui prend deux nombres en entrée,
// renvoie le résultat de leur addition ,soustraction, et multiplication.

function calcul(nmb1, nmb2) {
    let sum;
    let multiply;
    let substract;
    // pour aller plus loin :
    // comment pourrais-je faire pour améliorer mon algo pour que dans la soustraction,
    // on vérifie d'abord que nmb1 est plus grand que nmb2 ... ?
}

calcul(3, 59);
// ATTENDU : the result is : SUM = 62, substraction = -56, multiplication = 177
calcul(56, 759);
// ATTENDU: the result is : SUM = 815, substraction = -703, multiplication = 42504
calcul(299, 129359);
// ATTENDU: the result is : SUM = 129658, substraction = -129060, multiplication = 38678341
