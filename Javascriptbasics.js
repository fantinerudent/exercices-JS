// Variables //

// Qu'est ce qu'une déclaration?

// Qu'est ce qu'une initialisation?

// Qu'est ce qu'une affectation?

// Quels types de données?

// 10 = ?

// true = ?

// 'heart' = ?

// ['heart', 'ear', 'liver'] = ?

// 45.345654 = ?

// null = ?

// que va me retourner le script :

// if (a == b) {
//     alert(`it is true that ${a} = ${b}`)
// } else {
//     alert(`it is false that ${a} = ${b}`)
// }

let a = 10;
let b = "10";
let c = 7;

// if (a === c &&  a !== b)  {
//     alert(`the conditions are complete`)
// } else {
//     alert(`the conditions are not complete`)
// }

// Les tableaux;

// quelle méthode pour ajouter un objet à un tableau déjà initialisé.

// quelle méthode pour supprimer le dernier objet à un tableau déjà initialisé.

// je veux connaitre le nombre d'objets dans mon tableau sans avoir à les compter à la main...

// les boucles & conditions:

// Pour l'obtention de mon baccalauréat, je dois courir 10 tours de stade,
// quel script dois-je écrire pour compter mon nombre de tours, quelles sont les conditions
// pour que mon compteur s'arrete.

// fonctions:

// mon script a tellement bien fonctionné que le rectorat me demande d'en faire une fonction
// pour que tous les eleves puissent compter leur nombre de tours.
// comment créer une fonction qui recoit en argument un nombre qui peut être différent

function calculLaps(numberOfLapsNeeded) {}

calculLaps(50);
calculLaps(4);
calculLaps(300);
